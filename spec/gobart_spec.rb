require "spec_helper"

RSpec.describe Gobart do
  it "has a version number" do
    expect(Gobart::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
